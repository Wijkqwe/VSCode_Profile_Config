#include <bits/stdc++.h>

using namespace std;

void solve()
{
    if (1)
    {
        cout << '\n';
        return;
    }

    cout << endl;
    while (1)
    {
    }
}

signed main()
{
    ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
    int T;
    cin >> T;
    while (T--)
    {
        solve();
    }

    return 0;
}